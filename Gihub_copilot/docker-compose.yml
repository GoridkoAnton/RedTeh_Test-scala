services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_DB: airflow
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: airflow
    volumes:
      - ./pg-init:/docker-entrypoint-initdb.d:ro
    ports:
      - "5432:5432"

  airflow:
    image: apache/airflow:2.7.1
    environment:
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__CORE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
      AIRFLOW__CORE__FERNET_KEY: "fernet_key_placeholder"
      AIRFLOW__CORE__LOAD_EXAMPLES: "False"
    depends_on:
      - postgres
    volumes:
      - ./dags:/opt/airflow/dags
      - ./logs:/opt/airflow/logs
      - ./plugins:/opt/airflow/plugins
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/data
    ports:
      - "8080:8080"
    command: >
      bash -c "airflow db upgrade &&
               airflow users create --username admin --firstname Admin --lastname User --role Admin --email admin@example.com --password admin ||
               true &&
               airflow scheduler & airflow webserver"

  compact-parquet:
    build:
      context: .
      dockerfile: Dockerfile.spark.multistage
    image: compact-parquet:latest
    volumes:
      - ./data:/data
    # сервис обычно используется через docker compose run для генерации/compact,
    # но монтирование ./data:/data гарантирует, что все файлы появятся на хосте.

# (опционально можно добавить named volumes здесь)